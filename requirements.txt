# Core UI & data
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# ML / NLP (lightweight — sentence-transformers pulls torch automatically)
scikit-learn>=1.3.0
sentence-transformers>=2.6.0

# Export
openpyxl>=3.1.0
xlsxwriter>=3.1.0
reportlab>=4.0.0

# Data generation (demo dataset)
faker>=24.0.0

# Testing (not required for Cloud, but harmless)
pytest>=7.4.0
